<%= form_for @content, html: { multipart: true } do |f| %>
  <% if @content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content.errors.count, "error") %> prohibited this content from being saved:</h2>

      <ul>
      <% @content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!-- <div class="field">
    <label class="grey-text">Upload a picture of the area you want to share</label>
    <p><%= f.file_field :attachment %></p> 
 </div> -->
  <br>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "materialize-textarea" %>
  </div>

  
  <div class="field">
    <%= f.label :price_per_hour_minimum_50_cent %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    
    <%= f.file_field :cover %>
  </div>
    <br>
  <div class="actions">
    <%= f.submit 'Upload content', class: "btn btn-form" %>
  </div>

<br>

<% end %>
